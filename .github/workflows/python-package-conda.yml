name: Build LabAI

on: [push, pull_request]

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r .github/workflows/requirements.txt

    - name: Create Executable
      uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.10'
        spec: '.github/workflows/spec.spec'
        requirements: '.github/workflows/requirements.txt'
        upload_exe_with_name: 'LabAI-macos'
        options: --onefile, --name "LabAI", --windowed  # --windowed ensures no console

    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: LabAI-macos
        path: ./dist

  build-linux:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r .github/workflows/requirements.txt

    - name: Create Executable
      uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.10'
        spec: '.github/workflows/spec.spec'
        requirements: '.github/workflows/requirements.txt'
        upload_exe_with_name: 'LabAI-linux'
        options: --onefile, --name "LabAI", --windowed  # --windowed ensures no console

    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: LabAI-linux
        path: ./dist

  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r .github/workflows/requirements.txt

    - name: Create Executable
      uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.10'
        spec: '.github/workflows/spec.spec'
        requirements: '.github/workflows/requirements.txt'
        upload_exe_with_name: 'LabAI-windows'
        options: --onefile, --name "LabAI", --windowed  # --windowed ensures no console

    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: LabAI-windows
        path: ./dist
